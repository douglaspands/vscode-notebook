[tool.poetry]
name = "vscode-notebook"
version = "0.1.0"
description = "Projeto de Jupyter Notebook configurado n VSCode"
authors = ["douglaspands"]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^1.5.2"
selenium-wire = "^5.1.0"
pydash = "^5.1.2"
requests = "^2.28.1"

[tool.poetry.dev-dependencies]
ipykernel = "^6.20.0"
black = "^22.12.0"
isort = "^5.11.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
